import React from "react"

type TransactionBadgeProps = {
  label: string
  icon: React.ReactNode
  styles?: {
    iconContainer?: string
  }
}

export function TransactionBadge({ label, icon, styles }: TransactionBadgeProps) {
  return <div className=""></div>
}
